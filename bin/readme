To run the mgp version of the dbsnpload
1. Run filter_vcf_snps.sh - this filters the unzipped source file into individualchromosome files used as input to the loader
2. need to install pgsnpdbschema product
3. need to migrate to text to accomodate >50 subsnp id's 
*    ALTER TABLE snp_accession ALTER COLUMN accid TYPE text;
*    ALTER TABLE snp_accession ALTER COLUMN prefixpart TYPE text;
4. need to add strain translations - see translationload file and config file in http://wts.informatics.jax.org/wts2_projects/1200/WTS2-1253/sanger_straintrans/
5. run addSnpStrainMGP.csh - this adds new mgp strains to snp_strain
5. run mgpload.sh
