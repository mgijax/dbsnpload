To Install dbsnpload:

1) cp dbsnpload.config.default dbsnpload.config
   and edit as necessary. 
   
   If you change the dbsnpload.config value for RUNTIME_DIR or SNP_DATA_DIR 
   you must also change it in fxnClassDag.rcd

2) DLAInstall common.config.sh dbsnpload.config

A typical Install is done from dbsnpload root directory as follows:
/usr/local/mgi/live/dbutils/dlautils/DLAInstall /usr/local/mgi/live/etc/common.config.sh dbsnpload.config

